apiVersion: v1
kind: Service
metadata:
  name: notification-service
  labels:
    app: notification-service
    monitoring: enabled
spec:
  selector:
    app: notification-service
  ports:
    - name: http
      port: 8085
      targetPort: 8085
  type: ClusterIP