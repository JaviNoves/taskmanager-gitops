apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-task
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-task
  template:
    metadata:
      labels:
        app: mysql-task
    spec:
      containers:
        - name: mysql-task
          image: mysql:8.0
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_DATABASE
              value: taskdb
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_ROOT_PASSWORD
          volumeMounts:
            - mountPath: /var/lib/mysql
              name: mysql-task-storage
      volumes:
        - name: mysql-task-storage
          persistentVolumeClaim:
            claimName: mysql-task-pvc