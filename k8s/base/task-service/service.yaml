apiVersion: v1
kind: Service
metadata:
  name: task-service
  labels:
    app: task-service
    monitoring: enabled
spec:
  selector:
    app: task-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
  type: ClusterIP