apiVersion: v1
kind: Service
metadata:
  name: project-service
  labels:
    app: project-service
    monitoring: enabled
spec:
  selector:
    app: project-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
  type: ClusterIP